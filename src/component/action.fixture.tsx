import {Action} from "./action.tsx"

export default {
	"Primary": (
		<Action variant="primary">
			Primary Action
		</Action>
	),
	"Secondary": (
		<Action variant="secondary">
			Secondary Action
		</Action>
	),
}
